apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-reactjs.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-reactjs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-reactjs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
