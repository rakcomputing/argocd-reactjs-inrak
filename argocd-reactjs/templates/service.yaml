apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-reactjs.fullname" . }}
  labels:
    {{- include "argocd-reactjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-reactjs.selectorLabels" . | nindent 4 }}
